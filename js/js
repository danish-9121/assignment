js (only synchoronous js is included)
-word vs keyword
-var const let
--the difference
-- window object
--browser context api
--stack
--heap memory
--execution context
--lexical environment

-hoisting
-types in js
--how to copy reference values

-conditionls
-if else else-if
--truthy vs falsy

-loops
-for while
--foreach forin (forof do-while) rarely used

-functions
--callback fncs (asynchoronus h)
--what is first classs fncs

-params, arguments
-arrays
-push pop shift unshift
--how arrays are made behind the scenes
--why we can make negative indexes arrays in js
--practice  questions and sxenarios
--objects